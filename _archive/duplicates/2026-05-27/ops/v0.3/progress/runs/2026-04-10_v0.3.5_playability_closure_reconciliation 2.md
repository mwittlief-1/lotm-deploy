# 2026-04-10 v0.3.5 Playability Closure Reconciliation

## Summary

- The attached `v0.3.5 Playability Closure Plan` is directionally sound and matches the current repo seam strategy: carry-over gate first, lane-owned delivery second, integrator-only orchestration last.
- The carry-over gate is still live on kickoff truth. `V03-R4-006-T01` is accepted, while `V03-R4-004-T01` and `V03-R4-005-T01` are still `ready` and do not yet have landed handoff reports on this branch.
- Before this plan is converted into canonical `R5` backlog entries, the CPO feedback should be folded in as explicit acceptance criteria rather than left as implied intent.

## Current Repo Truth

### Carry-over frontier

- `ops/v0.3/progress/latest.yaml` still points at `V03-R4-004-T01` as the current task and `V03-R4-006-T01` as the last completed task.
- `ops/v0.3/backlog.yaml` marks:
  - `V03-R4-004-T01` as `ready`
  - `V03-R4-005-T01` as `ready`
  - `V03-R4-006-T01` as `done`
- `ops/v0.3/progress/runs/V03-R4-006-T01.md` confirms the UI raw-receipts surface now renders structured rows with `receipt_id`, `category`, `asset`, `delta`, and `counterparty` details when present.

### Receipts and invariants baseline

- Structured fiscal receipts already exist as a live contract in `src/sim/domains/economy/receipts.ts`, `src/sim/domains/economy/ledger.ts`, and the related tests under `tests/sim/`.
- The repo already has stable ordering and field-shape coverage for structured receipts in:
  - `tests/sim/fiscal_receipts_contract.test.ts`
  - `tests/sim/ledger_domain.test.ts`
- What is not yet explicit in the carry-over task wording is an end-to-end invariant of the form:
  - `sum(receipts grouped by asset) == resulting state delta by asset`
- If `V03-R4-005-T01` does not land that invariant as part of its focused test pass, `R5` should add it explicitly before receipt-heavy UI surfaces deepen further.

### Version-stamping nuance

- Core run creation already stamps runtime app version from `src/version.ts` through `src/sim/state.ts`.
- Today `src/version.ts` is `v0.3.4`, so the main initializer is not still hard-coded to `v0.2.9`.
- However, `docs/releases/v0.3.4_HARDENING_PLAYTEST_GUIDE.md` still documents an accepted limitation that packet exports may carry `app_version: v0.2.9` in run summary JSON on this branch line.
- Treat this as an unresolved evidence/export consistency issue, not as proof that `createNewRun()` is wrong.

### Obligations terminal semantics

- The runtime already has deterministic dispossession semantics:
  - `src/sim/phases/phase_succession.ts` sets `state.game_over = { reason: "Dispossessed", ... }` when `state.manor.unrest >= 100`
  - this happens in close-turn flow after `applyCloseTurnObligationsPhase(...)` and succession resolution, before incrementing the next turn
- The current obligations experience layer already exposes stage-aware pressure copy, including stage-three dispossession danger, through:
  - `src/sim/domains/experience/obligationsView.ts`
  - `src/ui/playScreenObligations.ts`
  - shared content slots in `src/content/experienceContent.ts`
- The missing piece is not raw engine behavior; it is making the modal spell out the terminal stage and the exact evaluation boundary clearly.

### Balance closure baseline

- `V03-R4-002` already landed a real balance-control seam:
  - one tuning table
  - one DOE harness
  - one deterministic regression seed pack
- The accepted docs are:
  - `docs/releases/v0.3.4_BALANCE_HOOK_PACKET.md`
  - `ops/v0.3/progress/runs/V03-R4-002.md`
- Those artifacts lock comparison points, but they do not yet convert them into explicit v0.3.5 acceptance bands or automatic runaway detectors.

## Required Tightenings Before Backlog Conversion

### 1. Keep the carry-over gate real

- `R5` should not open until `V03-R4-004-T01`, `V03-R4-005-T01`, and the already-accepted `V03-R4-006-T01` are reconciled on kickoff and the required gates are rerun.
- When checking `V03-R4-005-T01`, confirm it delivers:
  - structured receipt objects, not only string notes
  - a receipt-to-state-delta invariant, or an explicit follow-on task that adds it
  - resolved run-summary or export version-stamping consistency if that mismatch still reproduces

### 2. Fix the path shape in the plan

- The plan should use the repo-relative path `src/ui/panels/NewRunScreen.tsx`, not an absolute local path.

### 3. Make terminal obligations semantics explicit in `R5-008`

- The obligations transparency work should explicitly show:
  - the full stage ladder through terminal risk
  - the rule that actually ends the run
  - the phase boundary where that rule is evaluated
- The current runtime truth is:
  - arrears and enforcement pressure are accumulated through the obligations close-turn path
  - dispossession game over is evaluated in succession close-turn flow when unrest reaches `100`

### 4. Make maintenance labor drag auditable in `R5-006`

- `R5-006` should not stop at silent labor suppression.
- Acceptance should require either:
  - typed receipts for maintenance labor pressure, or
  - a deterministic capacity-pressure registry/snapshot that is surfaced alongside receipts and explain views
- This matters for player clarity now and for later v0.4 delegation economics.

### 5. Keep presets on a single init path in practice

- `R5-009` should require `applyPlayabilityPreset` to operate through one canonical new-run initialization seam.
- The preset seam should:
  - mutate one canonical run init DTO or state patch
  - never bypass normal `createNewRun()` worldgen, migrations, or registry wiring
  - stamp `seed` and `preset_id` into the run shell and export surfaces

### 6. Add explicit stability closure to `R5`

- The attached plan closes visible playability well, but v0.3.5 also needs a small stability closure slice.
- Add one `R5` epic or equivalent control-plane checkpoint for:
  - KPI acceptance bands
  - runaway detectors
  - a compare-against-baseline review flow using the existing DOE and regression seed pack
- The first detector set should at minimum cover:
  - coin runaway
  - starvation spiral / food-collapse pressure
  - arrears too soft
  - arrears too hard
  - manor-count growth staying frozen when it should not

## Recommended Shape For Future `R5` Backlog Authoring

- Preserve the attached epic split as the main delivery plan:
  - map view
  - manor view
  - house dossier
  - person cards
  - outbound marriage
  - maintenance realism
  - court provisioning
  - obligations transparency
  - preset pack and UAT lockability
- Add one small balance/stability closure epic after those, rather than smearing balance acceptance text across every playability epic.
- Write the `R5` backlog so these are acceptance criteria, not side notes.

## Integrator Readout

- I should treat the attached plan as the working v0.3.5 closure skeleton.
- I should also treat the CPO feedback as binding integration guidance before creating `V03-R5-*` backlog entries.
- The next honest control-plane move is not to dispatch `R5` yet; it is to finish and reconcile the `R4-004` and `R4-005` carry-over gate, then author `R5` with the tightenings above baked in.
