# v0.2.3.2 Regression Notes

**Date (UTC):** 2026-02-16  \
**QA Owner:** QA Engineer

## Summary

- **Gate result:** **CERTIFIABLE PASS** (both `npm run preflight` and `npm run qa` succeed without `node_modules/`).
- **Patch focus (per BUILD_INFO):** DevB correctness fixes + DevA UI polish; determinism maintained; no scope expansion.

## QA signals observed

### Preflight gate (`npm run preflight`)
- Produced: `qa_artifacts/v0.2.3.2_preflight.json`
- Result: **3/3 passed**

### QA gate (`npm run qa`)
- Produced: `qa_artifacts/v0.2.3.2_no_deps_gate.json`
- Result: **7/7 passed**
- Determinism, invariant clamps, bounded snapshots, policy registry drift, and migration smoke all passed.

## High-signal watch items (non-blocking)

1) **Golden seed suite selection drift**
   - v0.2.3.2 no-deps gate loaded golden seeds from `docs/golden_seeds_v0.2.1.json`.
   - The repo contains `docs/golden_seeds_v0.2.3.json`, but `scripts/qaNoDeps.mjs` only looks for `docs/golden_seeds_${APP_VERSION}.json` (exact match) then falls back to v0.2.1.
   - Recommendation: include v0.2.3 (and v0.2.2) in the candidate list, or ship `docs/golden_seeds_v0.2.3.2.json` so patch releases inherit the most recent seed suite automatically.

## BUILD_INFO.sim_version check

`sim_version = "mvp_v0_1"` matches `src/sim/version.ts` and the compiled sim in `dist_batch/`. This appears intentional for v0.2.3.2.
