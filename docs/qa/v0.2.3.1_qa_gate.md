# v0.2.3.1 QA Gate — CERTIFIABLE PASS

**Version tested:** v0.2.3.1 (CTO HEAD as provided in `lotm_v0.2.3.1_repo_HEAD.zip`)  
**Date:** 2026-02-15  
**Gate owner:** QA Engineer  
**Result:** **CERTIFIABLE PASS**

## Build identity (from `docs/BUILD_INFO.json`)

- **app_version:** `v0.2.3.1`
- **code_fingerprint:** `c4341557e729a0ad860425e6907ac6d8473fa5f82cb2713cb300dd9baaccd90d`
- **sim_version:** `mvp_v0_1`

## Environment

- Node: `v22.16.0`
- npm: `10.9.2`
- **No `node_modules/` present** (gate executed via no-deps fallback)

## Commands executed (authoritative)

From repo root:

```bash
npm run preflight
npm run qa
```

## Results

### Preflight

- `npm run preflight` → **PASS**
- Machine-readable output written:
  - `qa_artifacts/v0.2.3.1_preflight.json`

### QA Gate

- `npm run qa` → **PASS**
- Observed: **no-deps fallback** activated (`Vitest not found (node_modules missing)`)
- Machine-readable output written:
  - `qa_artifacts/v0.2.3.1_no_deps_gate.json`

## Coverage (what the no-deps gate verifies)

- Determinism: same seed + same policy ⇒ identical hashes/logs
- Invariants:
  - no NaN/Infinity
  - clamps hold (0–100 for unrest/relationship metrics when present)
  - energy/resources non-negative
  - allowed game-over reasons only
- Golden seed regression: runs the golden seed suite for the required horizon (15 turns), per gate implementation

## Notes / minor coverage drift

- The gate loaded golden seeds from **`docs/golden_seeds_v0.2.1.json`** (n=8).
  - This is **not blocking** for v0.2.3.1 certification (gate still ran deterministically and passed),
    but it is a coverage drift risk if v0.2.3+ is expected to maintain its own seed expectations.

## sim_version freshness check

`docs/BUILD_INFO.json.sim_version` is `mvp_v0_1` and matches:
- `src/sim/version.ts`
- `dist_batch/src/sim/version.js`

This appears **intentional / not stale** for v0.2.3.1.
