# v0.2.7 QA Gate — CERTIFIABLE PASS

**Version tested:** v0.2.7 (Repo HEAD zip provided)

**Build identity (from `docs/BUILD_INFO.json`):**
- **app_version:** v0.2.7
- **code_fingerprint:** 42422a45b0b2aa9903f904c622ce8ba5bedaf5dab9bde64f2ad521624ed55b2e
- **sim_version:** mvp_v0_1

## Execution mode
- **Clean / no-deps state:** `node_modules/` absent
- Gate executed via **no-deps fallback** (Vitest not present)

## Commands executed (repo root)
```bash
npm run preflight
npm run qa
```

## Results
- `npm run preflight` → **PASS (passed 3/3)**, exit code 0
  - Artifact: `qa_artifacts/v0.2.7_preflight.json`
- `npm run qa` → **PASS (passed 7/7)**, exit code 0
  - Console: `Vitest not found (node_modules missing). Running no-deps QA gate...`
  - Artifact: `qa_artifacts/v0.2.7_no_deps_gate.json`

## Required artifacts present
- `qa_artifacts/v0.2.7_preflight.json` ✅
- `qa_artifacts/v0.2.7_no_deps_gate.json` ✅

## Determinism + invariants coverage (no-deps gate)
- Determinism: same seed + same policy ⇒ identical logs/hashes
- Invariants: no NaN/Infinity; clamps (unrest/relationships 0–100); energy/resources non-negative; allowed game-over reasons only
- Golden seeds: executed for fixed horizon (15 turns) per gate contract

## Map v1 lane
- Map v1 gates **not present** in this build; no map:validate/assets:validate/boundary:check required for v0.2.7.

## sim_version check
`docs/BUILD_INFO.json.sim_version` is **mvp_v0_1** and matches `src/sim/version.ts`.
No evidence of staleness for v0.2.7.

## Release decision
**APPROVE v0.2.7** from QA.
