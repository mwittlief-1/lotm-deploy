# v0.3.4 Hardening Final Report

**Scope:** close the `V03-R4-001` hardening epic with one release-facing summary of what changed, what stayed intentionally stable, and what risks remain acceptable on the current branch line.

## 1. Accepted task sequence

The hardening epic closed through these accepted tasks:

- `V03-R4-001-T01` — hardening audit and hotspot map
- `V03-R4-001-T02` — widowhood, living-spouse, and remarriage repair
- `V03-R4-001-T03` — canonical heir repair and orphaned spouse-edge handling
- `V03-R4-001-T04` — hotspot simplification with no further replay drift
- `V03-R4-001-T05` — playtest and bug-reporting docs refresh

## 2. Accepted replay line

The current accepted replay batch hash for the post-hardening line is:

- `f850077e9f6a7338869f9861a6db0fc7e27f3c8cae56b6589e09b42dcc0d67f1`

This supersedes the pre-hardening line that still tolerated the stale widowhood and succession graph issues.

## 3. Accepted behavior diffs

These are intentional and should now be treated as correct:

- widow or widower status stays consistent across roster and related read models
- widowed subjects can remarry when no living spouse remains
- stale dead-spouse edges no longer block remarriage eligibility
- canonical heir resolution no longer trusts a stale persisted `house.heir_id`
- inheritance-claim suppression follows canonical heir nullability instead of stale pointer state
- succession spouse selection ignores ended spouse edges and keeps the active living spouse

## 4. Non-goals preserved

The hardening pass did **not** do these things:

- no turn-order rewrite in `src/sim/turn.ts`
- no broad inheritance-design expansion
- no new packet automation beyond the existing playtest-ops shell
- no UI contract rewrite beyond documentation and already accepted playtest-ops work

## 5. Remaining known limitations

These remain acceptable on the current branch:

- playtest packet assembly is still manual
- packet exports still surface `app_version: v0.2.9` on this branch line
- KPI snapshots remain placeholders rather than measured dashboards
- replay batches still emit accepted soft time-ceiling warnings for some long seeds
- the family-graph work is targeted hardening, not a full lineage-system redesign

## 6. Release-readiness note

`V03-R4-001` is ready to mark done. The hardening pass corrected the targeted widowhood, remarriage, and succession graph failures, kept later cleanup non-perturbative, and left one explicit playtest/reporting contract for final review.
