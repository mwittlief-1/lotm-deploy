# v0.3.0 TASKS — Integrator
**Last Updated:** 2026-03-23

Authoritative update:
- `docs/releases/v0.3_SCOPE_LOCK.md` overrides this document on product scope conflicts.
- `ops/v0.3/backlog.yaml` is the canonical machine-readable task queue for unattended work.

## Immediate Sequence
1. Land repo rails and codemap guidance.
2. Create branch scaffolding for `v0.3` work and keep `v0.2final` isolated.
3. Add phase/domain/UI directory scaffolds.
4. Verify baseline gates on the kickoff branch.
5. Begin behavior-preserving extraction of `src/sim/turn.ts` into `src/sim/phases/*`.

## Lane Preconditions
- `engine-core` lane starts only after phase wrappers exist.
- `social-mechanics` lane starts only after relationship-write boundaries are clear.
- `ui-reporting` lane starts only after `src/ui/panels/` is the agreed destination.
- `tooling-qa` lane can begin immediately on replay harness and golden-test improvements.

## Branch Naming
- Integrator: `codex/v0.3-refactor-kickoff`
- Engine core: `codex/v0.3-lane-engine-core`
- Social/mechanics: `codex/v0.3-lane-social-mechanics`
- UI/reporting: `codex/v0.3-lane-ui-reporting`
- Tooling/QA: `codex/v0.3-lane-tooling-qa`

## Review Rules
- Reject PRs that edit numbered duplicate files unless the PR is explicitly archive cleanup.
- Reject PRs that mix seam carving and mechanic redesign.
- Reject PRs that add new cross-domain writes without an owning API.
