# 2026-04-13 - v0.3.5 UI preset provenance acceptance

## Summary

Accepted the `UI`-owned preset selection and provenance shell onto the `v0.3.5`
control-plane line after integrating the lane packet into the acceptance
worktree and rerunning the deterministic merge-side checks.

Accepted task:

- `V03-R5-009-T04`

## Validation

- pass: merged focused acceptance bundle
  (`12/12` tests across preset pack, locked preset scenarios, new-run preset
  view, new-run screen, and run provenance banner seams)
- fail: `npm run preflight` with the standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave8-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave8-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped
- warning: fresh full-suite `qa` was attempted, but the runner hung in this
  environment without producing a fresh result and was terminated

## Notes

- The accepted UI packet keeps preset selection on the canonical
  `buildNewRunInit()` -> `applyPlayabilityPreset()` -> `createNewRun()` seam
  and surfaces the resulting preset and seed provenance in the new-run shell,
  play shell, and run log.
- The raw replay summary hashes differed
  (`982b7787fbe8eea5e4bc3a1cfeeaadcc7d2371a34d27b85cba4ec00b743222ec` and
  `a49e8c31fef7b0888001c1d5e3a836ddcfe29546f34f939c54c0e92a97b9fd72`), but the
  normalized hash matched exactly at
  `e519f5ce476960d955e228662db3b9d8e9f20594d60b7c217e72524b20bfb568`.
