# V03-R5-006-T03 - Maintenance labor helper seam only

## Summary

Delivered the maintenance-labor helper seam on `codex/v0.3-lane-engine-core` without touching integrator wiring. The court maintenance domain now exposes a deterministic helper that reads optional `maintenance_registry_v1` inputs, applies existing maintenance delegation scaling, and reports builder-first effective labor availability plus stable summary lines for later runtime/UI consumers.

## Changed Files

- `src/sim/domains/court/maintenance.ts`
- `tests/sim/maintenance_labor_helper.test.ts`

## Gate Summary

- pass: `npm test -- --run tests/sim/maintenance_labor_helper.test.ts tests/sim/delegated_maintenance.test.ts`

## Downstream Contract

- Call `buildMaintenanceLaborPressure(state)` from integrator-owned preview wiring once the maintenance registry writer is available on the active branch.
- Use `maintenanceLaborPressureSummaryLines(pressure)` if a human-readable receipt layer is needed, but keep that receipt emission in integrator-owned wiring rather than this helper branch.
- The helper reads `maintenance_registry_v1` from `state.economy`, `state.manor`, or top-level state and returns `null` when no active labor-bearing entries exist.
- The helper does not mutate the labor plan. It only reports `effective_farmers` and `effective_builders` after deterministic builder-first drag.

## Notes

- No edits were made to `src/sim/turn.ts` or `src/sim/phases/**`.
- No preview/runtime wiring was added here by request; this branch only ships the helper seam and tests.
