# 2026-04-13 - v0.3.5 locked preset manifest acceptance

## Summary

Accepted the tooling-owned locked preset scenario manifest onto the `v0.3.5`
control-plane line after integrating the lane packet and rerunning the
deterministic merge-side checks.

Accepted task:

- `V03-R5-009-T03`

## Validation

- pass: merged focused acceptance bundle
  (`16/16` tests across locked preset scenarios, outbound marriage, court provisioning, obligations, and preset pack seams)
- fail: `npm run preflight` with the standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave6-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave6-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped

## Notes

- The accepted manifest keeps one deterministic row per preset and derives live
  court-provisioning and outbound-marriage review cues from `createNewRun(seed)`
  plus accepted preview seams rather than introducing a second preset
  initialization path.
- Review expectations stay anchored to already-accepted receipt packets,
  obligations fixtures, court provisioning fixtures, UAT scenario rails, and
  maintenance reference artifacts.
- The raw replay summary hashes differed
  (`e3f852e8e6083c328d585815516e50ba671c94d8f628308d666679f28f3d8eb0` and
  `49a8b9a46da835de95dc0d78c9e94f35927fb6cbe5bb9cb5883869aeb70f6132`), but the
  normalized hash matched exactly at
  `e519f5ce476960d955e228662db3b9d8e9f20594d60b7c217e72524b20bfb568`.
