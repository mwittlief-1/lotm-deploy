# Run Log

**Run ID:** V03-R5-007-T04
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-tooling-qa
**Purpose:** Lock the court provisioning surface with deterministic fixtures and a reviewable UAT packet.

## Delivered scope

- Added `tests/fixtures/court_provisioning_snapshot_v0.3.5.json` as the compact provisioning contract covering baseline shortfall, carry-forward overrides, stipend receipt modes, and debug-order stability.
- Added `tests/support/courtProvisioningFixtures.ts` plus `tests/ui/courtProvisioningFixtures.test.tsx` to keep the accepted provisioning surface fixture-safe without opening a second runtime harness.
- Added `qa_artifacts/playtest_ops/v0.3.5/court_provisioning_uat_pack.json` and documented the packet in `docs/qa/court_provisioning_fixtures_v0.3.5.md`.

## Gate status

- Focused vitest suite: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- The packet stays read-only and remains anchored to the accepted provisioning and stipend seams.
- UAT cues point reviewers at player-tab ration behavior, carry-forward defaults, receipt-backed stipend rows, and deterministic debug ordering without inventing a new control plane.
