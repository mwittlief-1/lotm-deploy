# Run Log

**Run ID:** V03-R5-001-T05
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-tooling-qa
**Purpose:** Lock the accepted kingdom-map routing seam with deterministic fixtures and replay-safe ordering guards.

## Delivered scope

- Added `tests/fixtures/world_map_route_snapshot_v0.3.5.json` as the compact route-and-selection contract for the accepted map surface.
- Added `tests/ui/worldMapRouteFixtures.test.ts` to assert route hash stability, selected-manor round-tripping, and marker/debug ordering alignment.
- Documented the fixture contract in `docs/qa/world_map_route_fixtures_v0.3.5.md` so later UI work can extend the same seam without inventing a second route surface.

## Gate status

- Focused vitest suite: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- The fixture stays intentionally compact: route payload, routed selection summary, first marker ids, first debug-row ids, and selected marker/debug payloads only.
- Replay protection is ordering-focused, not export-focused, so later map UI changes can reuse the same guard without bloating replay artifacts.
