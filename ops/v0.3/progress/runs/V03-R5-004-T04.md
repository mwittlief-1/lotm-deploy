# Run Log

**Run ID:** V03-R5-004-T04
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-tooling-qa
**Purpose:** Lock the routed person-card modal with deterministic fixtures and origin-routing guards.

## Delivered scope

- Added `tests/fixtures/person_card_snapshot_v0.3.5.json` as the compact accepted person-card contract.
- Added `tests/support/personCardFixtures.tsx` and `tests/ui/personCardFixtures.test.tsx` to assert stable tab content, debug ordering, and canonical route origins across live trigger surfaces.
- Documented the fixture scope and routing guard in `docs/qa/person_card_fixtures_v0.3.5.md`.

## Gate status

- Focused vitest suite: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- The fixture remains compact and route-focused, so UI iteration can continue without freezing the full person registry.
- Route coverage stays tied to accepted live origins instead of introducing test-only modal entry points.
