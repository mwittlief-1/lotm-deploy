# Run Log

**Run ID:** V03-R5-004-T03
**Date:** 2026-04-11
**Branch:** codex/v0.3-lane-ui-experience
**Purpose:** Route every accepted UI name surface into the shared person-card modal without inventing new sim-side contracts.

## Delivered scope

- Added a shared person-card view/model pair that reads only from the accepted `person_card_registry` seam and renders overview, family, office or service, relationships, and debug tabs.
- Wired household, roster, prospects, known houses, and dossier surfaces to the same modal route so every accepted name tap lands in one deterministic UI flow.
- Kept the route strictly read-only against sim state: the modal drills into canonical preview seams and never mutates people, house, or relationship data directly.

## Acceptance notes

- Household summary and roster detail preserve distinct route origins, but they resolve the same modal shell and person-card surface.
- Relationship rows are sorted deterministically before truncation so the modal stays comparable across replay runs.
