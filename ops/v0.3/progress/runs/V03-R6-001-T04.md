# Run Log

**Run ID:** V03-R6-001-T04
**Date:** 2026-04-20
**Branch:** codex/v0.3-lane-ui-experience
**Purpose:** Add an explicit unrest causal walkdown and keep summary and drilldown surfaces aligned on the same reconciled unrest story.

## Delivered scope

- Reworked `src/sim/domains/experience/reporting.ts` so `turn_explanation_v1.unrest_walkdown` now emits deterministic, player-facing rows for starting unrest, arrears pressure, project pressure, event pressure, relief, net unrest change, and ending unrest.
- Aligned the shared headline cause wording to that same canonical unrest walkdown, so Diff Ledger, Turn Report, and Manor State all point at the same primary pressure and relief contributors.
- Updated `src/ui/panels/ManorStatePanel.tsx` plus its checked-in `src/ui/panels/ManorStatePanel.js` companion to render reconciled unrest cause rows and a start/end reconciliation line instead of drifting into raw debug-shaped buckets.
- Added focused coverage in `tests/sim/turn_explanation_contract.test.ts`, `tests/ui/turnReportPanel.test.tsx`, `tests/ui/diffLedgerPanel.test.tsx`, and `tests/ui/manorStatePanel.test.tsx` for the reconciled walkdown and shared headline copy.

## Verified example

- Reconciled unrest example: `Starting unrest -> Arrears pressure (+4) -> Project pressure: Bridge repairs (+2) -> Event pressure: Village Riot (+1) -> Relief: Harvest Festival (-1) -> Net unrest change (+6) -> Ending unrest`
- Shared headline wording: `Arrears pressure (+4) pushed unrest up while Relief: Harvest Festival (-1) eased it.`
- Exact surface path used to verify it: `turn_explanation_v1.headline_causes.unrest` plus `turn_explanation_v1.unrest_walkdown`, rendered through the Manor State unrest subcard and breakdown details.

## Gate status

- Focused vitest suite: PASS
- `npm run build`: PASS on the chained lane after this task and `V03-R6-007-T02`
- `npm run qa`: not run on this lane
- `npm run preflight`: not run on this lane
- `npm run seed:replay:batch`: not run twice on this lane
