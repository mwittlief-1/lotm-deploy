# 2026-04-12 - v0.3.5 social and UI offer/provisioning acceptance

## Summary

Accepted the next `social` and `UI` wave onto the `v0.3.5` control-plane line
 after integrating the lane commits into the acceptance worktree and rerunning
 the merge-side gates.

Accepted tasks:

- `V03-R5-005-T04`
- `V03-R5-007-T03`

## Validation

- pass: fresh full-suite vitest (`459/459`) from `npm run qa`
- pass: fresh UAT via `qa_artifacts/uat_gate.json`
- fail: `npm run preflight` with the standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave4-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave4-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped

## Notes

- The accepted social packet lands deterministic outbound offer resolution with
  canonical scouting gates, accept or reject outcomes, settlement placeholder
  routing, and evidence-safe relationship updates.
- The accepted UI packet renders a read-only court provisioning sheet from the
  accepted provisioning and stipend seams without reintroducing any write logic
  on the UI side.
- The raw replay summary hashes differed
  (`0adfac59622ba5d164566153f4624e5208bc3ab949ff425f5cbe9870c6bb203b` and
  `c7e9c63c8ecb0fc32d915f7944f30f0ecf5e1971739fd400faca7b5c5c7e4066`), but the
  normalized hash matched exactly at
  `e519f5ce476960d955e228662db3b9d8e9f20594d60b7c217e72524b20bfb568`.
