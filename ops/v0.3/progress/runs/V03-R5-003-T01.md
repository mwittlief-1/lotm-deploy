# V03-R5-003-T01 - Opening social dossier contract handoff

## Summary

Rebased the social opening-wave handoff onto `codex/v0.3.5-prep` at `5daea7a` and reduced the package to the `V03-R5-003-T01` scope only. The delivered change upgrades `house_dossiers[]` to `house_dossier_summary_v2` with knownness, richer A/R/T summaries, holdings footprint, coarse ledger posture, and prospect-house coverage, while keeping relationship reads inside canonical APIs.

## Changed Files

- ops/v0.3/progress/runs/V03-R5-003-T01.md
- src/sim/domains/people/knownHouseSummaries.ts
- src/sim/types.ts
- tests/sim/bounded_snapshot_contract.test.ts
- tests/sim/known_house_snapshot_fields.test.ts

## Contract Notes

- `known_houses[]` remains serialized as before.
- `house_dossiers[]` now uses `house_dossier_summary_v2`.
- Added fields:
  - `knownness`
  - `knownness_sources`
  - `kinship_tags`
  - `relationship_summary`
  - `holdings_footprint`
  - `ledger_band`
  - `ledger_trend`
- `src/sim/domains/experience/reporting.ts` was resolved by keeping the prep branch's current economy and world seams intact, so this handoff does not alter snapshot wiring beyond the richer dossier payload returned by `buildKnownHouseExperienceSurfaces`.

## Guardrails

- No direct relationship-store writes were added outside the relationship engine.
- No direct coin mutation was added.
- No lane changes were carried forward in `src/sim/turn.ts`, `src/sim/phases/phase_obligations.ts`, `ops/v0.3/backlog.yaml`, or `ops/v0.3/progress/latest.yaml`.

## Verification

- pass: npx vitest run tests/sim/known_house_snapshot_fields.test.ts tests/sim/bounded_snapshot_contract.test.ts
- pass: npm run qa
- fail: npm run preflight (`test3_mismatches=16`)
- pass: npm run seed:replay:batch -- --outdir=qa_artifacts/seed-replay-r5003-run1
- pass: npm run seed:replay:batch -- --outdir=qa_artifacts/seed-replay-r5003-run2
- pass: normalized replay summaries match exactly across both batch runs; differing `summary_hash` values were due only to the output directory path baked into `artifact_relpath`
- note: both replay batches reported soft timing overages against the `250ms` per-seed warning ceiling, but they completed successfully and produced matching run hashes
