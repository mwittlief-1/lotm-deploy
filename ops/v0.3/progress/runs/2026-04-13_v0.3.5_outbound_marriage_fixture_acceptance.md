# 2026-04-13 - v0.3.5 outbound marriage fixture acceptance

## Summary

Accepted the `tooling`-owned outbound marriage fixture and preset coverage rail
onto the `v0.3.5` control-plane line after integrating the lane packet into the
acceptance worktree and rerunning the deterministic merge-side checks.

Accepted task:

- `V03-R5-005-T06`

## Validation

- pass: merged focused script and test validation
  (`scripts/outboundMarriagePresetCoverage.ts`,
  `scripts/lockedPresetScenarios.ts`, and `12/12` tests across outbound
  marriage fixtures, replay guards, locked preset scenarios, and existing
  outbound marriage UI surfaces)
- fail: `npm run preflight` with the standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave10-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave10-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped
- warning: fresh full-suite `qa` was attempted, but the runner hung in this
  environment without producing a fresh merge-side result and was terminated

## Notes

- The accepted packet locks representative preset launch windows for outbound
  marriage without introducing a second init seam, then feeds those review cues
  into the locked preset manifest so preset closeout remains on one canonical
  checklist.
- The raw replay summary hashes differed
  (`5042bef755b30808e78669edda4b5f0e14fe5a2e32d0a5f9cdc1e8bd78647260` and
  `7ce448122dd0ccecd6e788a7d6a35478bcd7d0b62bce792b196e52cfa7ddd1a5`), but the
  normalized hash matched exactly at
  `e519f5ce476960d955e228662db3b9d8e9f20594d60b7c217e72524b20bfb568`.
