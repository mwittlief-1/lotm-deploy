# 2026-04-10 v0.3.5 Control Plane Activation

## Summary

- Added canonical `v0.3.5` release, epic, and task entries to `ops/v0.3/backlog.yaml`.
- Opened the first scheduler-ready task wave with these ready tasks:
  - `V03-R5-001-T01`
  - `V03-R5-003-T01`
  - `V03-R5-006-T01`
  - `V03-R5-009-T01`
- Updated `ops/v0.3/progress/latest.yaml` so `cursor.current_task_id` now points to `V03-R5-001-T01`.
- Kept later `R5` work in `todo` state so the queue matches the intended first-wave launch posture instead of over-opening every lane at once.

## Branch Posture

- Authoring branch: `codex/v0.3.5-prep`
- Locked baseline remains:
  - `codex/v0.3.4-locked`
  - tip `b23fbcc`
- Control-plane intent is to fast-forward the canonical integrator branch to the `v0.3.5` prep line after this activation pass so branch policy remains coherent.

## Gates

- pass: `npm run ops:v0.3:validate`
- pass: `npm run ops:v0.3:scheduler-dry-run`
- pass: `npm run ops:v0.3:rebase-dry-run`

## Notes

- World topology work remains queued behind the kickoff-owned map renderer attachment checkpoint `V03-R5-001-T01`.
- The original maintenance labor-drag task shape was corrected to respect the integrator boundary:
  - `V03-R5-006-T03` now owns the engine-core helper seam only
  - `V03-R5-006-T03A` now owns the kickoff-only wiring into allocatable labor ordering
- The `R5` control plane also includes the added balance and stability closure epic `V03-R5-010` so acceptance bands and runaway detectors are part of the canonical closeout plan rather than side notes.
- Concurrent lane edits and duplicate-suffixed artifact noise are present in the shared worktree; this activation pass intentionally avoids staging or reverting any of those files.
