# 2026-04-10 - v0.3.5 social dossier acceptance

Accepted `V03-R5-003-T01` onto the v0.3.5 control-plane line from the rebased
social handoff commit `2f372ee`.

## Outcome

- `house_dossier_summary_v2` is accepted for live social surfaces.
- Prospect houses still resolve to populated dossier rows in the live contract.
- Bounded replay snapshots now serialize a deterministic 24-row dossier subset
  and rely on `known_houses[]` for duplicated identity fields so the
  `524288`-byte per-turn snapshot cap remains intact.

## Validation

- pass: `npm run qa`
- fail: `npm run preflight` with standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-social-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-social-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped

## Notes

- Raw replay `summary_hash` values differ between the two runs only because the
  summary payload includes output-path metadata.
- The bounded snapshot contract now explicitly locks the capped dossier payload
  shape while leaving the live preview-state dossiers fully populated for UI
  work.
