# Run Log

**Run ID:** V03-R5-009-T04
**Date:** 2026-04-13
**Branch:** codex/v0.3-lane-ui-experience
**Purpose:** Add preset selection and preset/seed provenance to the new-run shell without creating a second initialization path.

## Delivered scope

- Added `src/ui/newRunPresetView.ts` so the new-run shell reads the accepted playability preset pack, exposes repo-relative source provenance, and always builds launches through `buildNewRunInit()` plus `applyPlayabilityPreset()` on the canonical `canonical_new_run_init_v1` seam.
- Expanded `src/ui/panels/NewRunScreen.tsx` and `src/App.tsx` so operators can select a locked preset, see its seed/policy/source refs, and launch through `createNewRun(buildCanonicalNewRunInit(...))` rather than a second preset-only initializer.
- Added shared run provenance surfaces in `src/ui/runProvenanceView.ts` and `src/ui/panels/RunProvenanceBanner.tsx`, then mounted them in the play shell and run log so seed and preset provenance stay visible after launch.
- Added focused coverage in `tests/ui/newRunPresetView.test.ts`, `tests/ui/newRunScreen.test.tsx`, and `tests/ui/runProvenanceBanner.test.tsx` for canonical wiring, repo-relative provenance, and locked-preset versus custom-seed rendering.

## Gate status

- Focused vitest suite: PASS
- `npm run build`: PASS
- `npm run qa`: not run on this lane
- `npm run preflight`: not run on this lane
- `npm run seed:replay:batch`: not run twice on this lane

## Contract handoff

- Preset selection is strictly a UI/control-plane concern; the only launch path remains `buildNewRunInit()` -> `applyPlayabilityPreset()` -> `createNewRun()`.
- Run/export provenance now carries the seed and preset id surfaced from canonical run state, so later ops/export work can read one consistent source of truth.
