# Escalation Report

**Task ID:** V03-R0-003-T02
**Date:** 2026-03-26
**Tier:** E2
**Branch:** codex/v0.3-lane-engine-core
**Commit:** 06c195c7753fe53f35ce0a206611efd0bc155fed
**PR URL:** 

## What happened
- Claimed `V03-R0-003-T02` and added a court-domain decision-budget scaffold in `src/sim/domains/court/decisionBudget.ts`.
- Added focused tests in `tests/sim/decision_budget_domain.test.ts` plus a registry contract note in `docs/qa/court_decision_budget_registry_v0.3.0.md`.
- Ran `npm run ops:v0.3:validate`, which passed after reconciling the lane claim into `active_claims`.
- Ran the required gates until the first escalation stop: `npm run preflight` passed and `npm run qa` failed, so the run stopped before `npm run seed:replay:batch` twice.

## Evidence
- Gate results:
  - `ops:v0.3:validate`: PASS
  - `qa`: FAIL
  - `preflight`: PASS
  - `seed_replay_batch_twice`: not run
- Replay hashes:
  - not run for this task
- Golden diff summary:
  - no golden replay was run for this task
- Environment notes:
  - `qa_artifacts/v0.2.9_no_deps_gate.json` reports three failures, all caused by `snapshot_before forbidden key: institutions`

## Why this is an escalation
- `V03-R0-003-T02` lists `non_perturbation_failure` as an escalation trigger.
- `ops/v0.3/escalation-policy.yaml` classifies `non_perturbation_failure` as `E2_stop_immediately`.
- The new registry module is not yet wired into turn execution, so the failing bounded-snapshot check appears to be inherited from the shared baseline rather than introduced by this task's implementation files.

## Proposed options
1. Fix the shared bounded-snapshot regression first, then reset `V03-R0-003-T02` to `ready` and rerun this lane task.
2. Move the shared QA failure into a tooling/integrator task and keep the engine-core implementation parked until the baseline is green again.

## Recommended next action
- Stop lane execution here and hand the bounded-snapshot failure back to the owner of the shared QA baseline before selecting another engine-core task.
