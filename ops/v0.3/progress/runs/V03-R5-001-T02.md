# Run Log

**Run ID:** V03-R5-001-T02
**Date:** 2026-04-10
**Branch:** codex/tmp-v0.3.5-acceptance
**Purpose:** Accept the world-owned map selector seam from the frozen topology bundle.

## Delivered scope

- Added deterministic world-owned map selector support from the frozen XMAP bundle.
- Exposed stable seat coordinate, ownership, holding, county, and transport selector data through `getMapViewSelectorRow(...)`.
- Kept selector ordering deterministic and anchored to canonical manor ids.

## Acceptance notes

- The selector seam ships independently from the compact serialized map snapshot so bounded replay snapshots stay under the byte cap.
- Explicit empty road and river exposure states remain available from the selector row seam.
