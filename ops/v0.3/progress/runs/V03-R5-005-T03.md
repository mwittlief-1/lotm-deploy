# Run Log

**Run ID:** V03-R5-005-T03
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-economy-fiscal
**Purpose:** Add canonical dowry and dower receipt placeholders so outbound marriage can stay on ledger-backed fiscal paths.

## Delivered scope

- Added `src/sim/domains/economy/marriageSettlement.ts` to define deterministic dowry and dower settlement scaffolds plus receipt-backed asset application across coin, food stores, and meat stores.
- Added `tests/sim/marriage_settlement.test.ts` to lock scaffold ordering, per-asset receipt emission, and shortfall handling.
- Kept the seam placeholder-only so later offer acceptance wiring can consume it without inventing direct coin or stores mutation paths.

## Gate status

- Focused vitest suite: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- Settlement scaffolds keep canonical asset ordering as `coin`, `food_stores`, then `meat_stores`.
- Every applied placeholder delta routes through ledger receipt APIs with stable `rule_id`, `category`, and counterparty metadata.
