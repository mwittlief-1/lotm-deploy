# Run Log

**Run ID:** V03-R5-010-T01
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-tooling-qa
**Purpose:** Turn the locked preset and DOE artifacts into explicit KPI acceptance bands for v0.3.5 closeout.

## Delivered scope

- Added `scripts/kpiAcceptanceBands.ts` to build `qa_artifacts/playtest_ops/v0.3.5/kpi_acceptance_bands.json` from the accepted preset pack, locked regression seed pack, receipt-bundle seed pack, and DOE summaries.
- Added `tests/sim/kpi_acceptance_bands.test.ts` to assert artifact determinism, metric coverage, and source anchoring.
- Documented the target-versus-comparison-rail contract in `docs/qa/kpi_acceptance_bands_v0.3.5.md`.

## Gate status

- Focused vitest suite: PASS
- `node node_modules/tsx/dist/cli.mjs scripts/kpiAcceptanceBands.ts`: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- The KPI artifact reuses the accepted preset pack instead of creating a second scenario catalog or second init seam.
- Each metric band separates pass/fail targets from comparison rails so operators can tell what is locked versus what remains reference-only.
