# V03-R6-008-T04 Acceptance

Date: 2026-04-21
Integrator branch: `codex/tmp-v036-integrator`
Accepted lane commit: `b5c35c3` (`chore(tooling): normalize v0.3.6 preflight baseline`)
Integrator fix commit: `7d7c13c` (`fix(tooling): restore run provenance helpers`)

## Accepted Scope

- Preflight now names the explicit v0.3.6 baseline contract instead of falling back silently to stale v0.2.x evidence.
- Run provenance helper files were restored because the tooling handoff referenced them but did not commit them.
- Provenance surfaces, run export, New Run, and Run Log continue to agree on build/version provenance.

## Evidence

- `npm run preflight`
- Result: `preflight: PASS (passed 3/3)`, `test3_mismatches=0`, baseline contract `docs/qa/v0.3.6_preflight_baseline_contract.json`.
- Focused Vitest integration run: 12 files passed, 32 tests passed.

## Notes

- This does not close `V03-R6-008-T06`; branch-wide `npm run qa` still needs a fresh clean-exit handoff.
