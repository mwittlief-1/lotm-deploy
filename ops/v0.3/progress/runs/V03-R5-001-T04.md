# Run Log

**Run ID:** V03-R5-001-T04
**Date:** 2026-04-10
**Branch:** codex/v0.3-lane-ui-experience
**Purpose:** Render the first player-facing kingdom map from accepted world seams without introducing new sim contracts.

## Delivered scope

- Replaced the placeholder map shell with a read-only kingdom map driven by the accepted `map_view_snapshot_v1` seam plus the existing world selector helper.
- Highlighted player holdings and the liege seat, routed manor clicks into Manor View v0, and kept the overlay table sorted by `manor_id`.
- Upgraded the Holdings map checkpoint so selected manors open the kingdom map when the canonical selector seam can resolve a concrete target.

## Acceptance notes

- UI routing stays read-only and only moves canonical map/manor targets.
- Synthetic fixtures still fall back to the anchor-only topology target when the richer selector seam is unavailable.
