# 2026-04-12 - v0.3.5 second-wave acceptance

## Summary

Accepted the next combined `UI`, `social`, and `tooling` wave onto the
`v0.3.5` control-plane line after integrating the lane packets into the
acceptance worktree and rerunning the merge gates.

Accepted tasks:

- `V03-R5-001-T05`
- `V03-R5-002-T04`
- `V03-R5-003-T03`
- `V03-R5-004-T03`
- `V03-R5-005-T02`
- `V03-R5-007-T01`
- `V03-R5-008-T02`
- `V03-R5-010-T01`

## Validation

- pass: fresh `npm run qa` (`445/445` vitest + fresh `npm run uat:gate`)
- fail: `npm run preflight` with the standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-second-wave-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-second-wave-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped

## Notes

- The accepted UI packet routes shared person-card modal entry points across
  the accepted live surfaces and deepens the obligations modal with terminal
  stage visibility and tangible-bite preview rows.
- The accepted social packet adds the outbound scouting registry plus court
  provisioning and stipend surfaces without crossing the integrator-only
  orchestration boundary.
- The accepted tooling packet locks the map, manor, and dossier seams with
  deterministic fixtures and adds KPI acceptance bands anchored to the locked
  preset and DOE artifacts.
- The normalized replay summary hash remained
  `9120b4cedf74d588c2da5295bfdce62bf342f5e6e99b035cb3981729965598f0`.
