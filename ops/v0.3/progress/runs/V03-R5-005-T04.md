# Run Log

**Run ID:** V03-R5-005-T04  
**Date:** 2026-04-12  
**Branch:** codex/v0.3-lane-social-mechanics  
**Purpose:** Resolve outbound marriage offers through canonical relationship, evidence, and fiscal seams on top of the accepted scouting and settlement contracts.

## Delivered scope

- Added terminal outbound-offer persistence to [marriageOfferRegistry.ts](/Users/matt_wittlief_home/Documents/GitHub/lotm-deploy-social-mechanics/src/sim/domains/people/marriageOfferRegistry.ts) so resolved outbound offers now fold back into the canonical marriage-offer registry state and reuse the existing reject-cooldown surface.
- Added [resolveOutboundMarriageOffer(...)](/Users/matt_wittlief_home/Documents/GitHub/lotm-deploy-social-mechanics/src/sim/domains/people/marriage.ts) in [marriage.ts](/Users/matt_wittlief_home/Documents/GitHub/lotm-deploy-social-mechanics/src/sim/domains/people/marriage.ts) with:
  - scouting-registry gating against non-ready candidates
  - deterministic accept vs reject scoring for outbound offers
  - blocked outcomes for missing subjects/candidates, ineligible subjects, cooldown collisions, same-sex offers, and settlement shortfalls
  - dowry and dower placeholder settlement routing through `marriageSettlement.ts`
  - spouse-edge, court-membership, household-membership, relationship, and runtime-evidence updates through canonical APIs only
- Added [outbound_marriage_offer_pipeline.test.ts](/Users/matt_wittlief_home/Documents/GitHub/lotm-deploy-social-mechanics/tests/sim/outbound_marriage_offer_pipeline.test.ts) to lock accepted, rejected, cooldown-blocked, and insufficient-assets-blocked outcomes.

## Contract handoff

- UI can submit a composed outbound `MarriageOffer` plus optional dowry/dower asset requests to `resolveOutboundMarriageOffer(...)` without touching phases or direct state mutation paths.
- Economy receives receipt-backed settlement results through `dowry_settlement_result`, `dower_settlement_result`, and aggregated `receipt_snapshots`.
- Social/registry consumers can read resolved outbound offer states and reject cooldowns through `buildMarriageOfferRegistryFromState(...)` and `getMarriageRejectCooldown(...)`.
- Relationship deltas, liege deltas, and runtime evidence all flow through canonical APIs; there are no direct relationship-store writes outside the relationship engine.

## Gate status

- Focused vitest suite: PASS
- Nearby marriage regression sweep: PASS
- `npm run qa`: inconclusive runner hang with no incremental output or fresh `qa_artifacts/vitest.json`; process was terminated instead of left orphaned
- `npm run preflight`: FAIL with inherited `test3_mismatches=16`
- `npm run seed:replay:batch`: PASS twice
- Replay comparison: per-run replay artifacts matched exactly across both runs; top-level `summary_hash` differed only because `summary.json` includes output-path metadata and its own file hash
- Replay timing: both batches emitted the usual soft warnings above the `250ms` ceiling but completed successfully
