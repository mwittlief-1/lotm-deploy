# Run Log

**Run ID:** V03-R5-003-T02
**Date:** 2026-04-10
**Branch:** codex/v0.3-lane-ui-experience
**Purpose:** Replace the summary-only known-house drilldown with the canonical dossier modal and debug tab.

## Delivered scope

- Added a house dossier modal routed by `house_id` and rendered strictly from the accepted `house_dossiers[]` read model.
- Replaced the known-house card drilldown affordance with an `Open dossier` action that only appears when the canonical dossier seam resolves for that house.
- Added a shared player/debug dossier surface so the modal exposes the same canonical fields in both narrative and raw deterministic forms.

## Acceptance notes

- The modal does not read raw house registries for dossier content; it consumes only the accepted dossier payload.
- The debug table uses a fixed field order so testers can compare dossier rows without guessing at object-key ordering.
