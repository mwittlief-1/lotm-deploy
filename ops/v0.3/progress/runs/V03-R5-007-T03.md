# Run Log

**Run ID:** V03-R5-007-T03
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-ui-experience
**Purpose:** Render the court provisioning modal from the accepted provisioning and stipend read models.

## Delivered scope

- Added a UI-only provisioning surface builder that reads the canonical `court_provisioning_view` and `court_stipend_registry` seams and projects summary cards, ration allocation rows, override rows, stipend rows, and deterministic debug tables.
- Added a `CourtProvisioningPanel` modal with player and debug tabs so ration policy, carry-forward state, stipend placeholders, and raw registry ordering are visible without mutating sim state.
- Wired a read-only entry point into the Decisions shell so the provisioning sheet is reachable alongside the existing court budget and obligations planning surfaces.

## Acceptance notes

- The UI does not recreate stipend or ration write logic; it only renders the accepted provisioning and fiscal policy seams already attached to preview state.
- Provisioning rows and stipend rows stay in deterministic `person_id` / `stipend_key` order, while the ration-policy section preserves the canonical allocation order from the fiscal policy.
