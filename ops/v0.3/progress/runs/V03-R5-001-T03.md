# Run Log

**Run ID:** V03-R5-001-T03
**Date:** 2026-04-10
**Branch:** codex/tmp-v0.3.5-acceptance
**Purpose:** Accept the bounded `map_view_snapshot_v1` seam for UI consumption.

## Delivered scope

- Added `map_view_snapshot_v1` to bounded snapshots with deterministic row ordering.
- Compact row serialization keeps the snapshot replay-safe while preserving route-critical ids and seat coordinates.
- The richer selector seam remains world-owned and test-covered outside the serialized snapshot payload.

## Acceptance notes

- Initial integration with the full selector row exceeded the replay byte cap.
- The accepted shape compacts serialized rows while preserving the richer selector API from `V03-R5-001-T02`.
