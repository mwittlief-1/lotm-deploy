# Run Log

**Run ID:** V03-R5-002-T04
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-tooling-qa
**Purpose:** Lock the accepted Manor View seam with deterministic anchor and routed-manor fixtures.

## Delivered scope

- Added `tests/fixtures/manor_view_surface_v0.3.5.json` covering both the current anchor manor and a routed non-anchor manor.
- Added `tests/ui/manorViewFixtures.test.tsx` to assert helper text, maintenance-summary behavior, nearest-manor ordering, terrain ordering, and debug hex-row ordering.
- Documented the fixture contract in `docs/qa/manor_view_fixtures_v0.3.5.md` for later manor-surface extensions.

## Gate status

- Focused vitest suite: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- The fixture explicitly locks the routed-manor case where maintenance totals must stay null instead of inheriting anchor-manor upkeep data.
- The paired test keeps player-tab and debug-tab ordering comparable across replay runs.
