# Run Log

**Run ID:** V03-R5-009-T03
**Date:** 2026-04-13
**Branch:** codex/v0.3-lane-tooling-qa
**Purpose:** Author the locked preset scenario manifest that ties the accepted v0.3.5 surfaces and review rails into one deterministic closure contract.

## Delivered scope

- Added `scripts/lockedPresetScenarios.ts` to generate `qa_artifacts/playtest_ops/v0.3.5/locked_preset_scenarios.json` from the accepted preset pack, live seed-derived court provisioning and outbound marriage surfaces, receipt-bundle review packets, obligations fixture locks, court provisioning fixture locks, UAT scenario rails, and maintenance reference artifacts.
- Added `tests/ui/lockedPresetScenarios.test.ts` to lock manifest determinism, one-row-per-preset coverage, live-seed surface cues, and source-backed review expectations.
- Documented the contract in `docs/qa/locked_preset_scenarios_v0.3.5.md` so later preset UI and operator work can consume one canonical closure manifest rather than inventing a second preset catalog.

## Gate status

- Focused vitest suite: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- The manifest derives live court-provisioning and outbound-marriage cues from `createNewRun(seed)` plus accepted preview seams, not from a fixture-only or preset-only initialization path.
- Packet, fixture, UAT, and maintenance expectations stay reference-linked to already-accepted artifacts so `playability_preset_pack_v1` remains the single preset control plane.
