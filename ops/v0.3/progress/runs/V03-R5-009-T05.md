# Run Log

**Run ID:** V03-R5-009-T05
**Date:** 2026-04-13
**Branch:** codex/v0.3-lane-tooling-qa
**Purpose:** Refresh the preset-driven UAT gate and operator docs so the locked preset pack is the canonical v0.3.5 closure checklist.

## Delivered scope

- Expanded `scripts/uatScenarioGate.ts` so the canonical gate emits `closure_checklist[]`, operator-facing preset guidance, source-artifact hashes, and release-scoped filtered artifacts for `--preset` reruns without replacing the full checklist file.
- Refreshed `qa_artifacts/playtest_ops/uat_scenario_gate.json` plus the preset-filtered `qa_artifacts/playtest_ops/v0.3.5/uat_scenario_gate__preset_*.json` packets, and re-anchored `qa_artifacts/playtest_ops/v0.3.5/locked_preset_scenarios.json` to the updated gate artifact.
- Updated `docs/qa/playability_preset_pack_v0.3.5.md` and `docs/qa/uat_scenario_pack_v0.3.md`, and added `docs/qa/preset_operator_checklist_v0.3.5.md` so operators use preset ids and provenance checks as the closeout review flow.

## Gate status

- Focused script validation: PASS
- `node node_modules/tsx/dist/cli.mjs scripts/uatScenarioGate.ts`: PASS
- `node node_modules/tsx/dist/cli.mjs scripts/uatScenarioGate.ts --preset=uat_arrears_enforcement`: PASS
- `node node_modules/tsx/dist/cli.mjs scripts/uatScenarioGate.ts --preset=uat_grant_visibility`: PASS
- `node node_modules/tsx/dist/cli.mjs scripts/uatScenarioGate.ts --preset=uat_hunting_proxy`: PASS
- `node node_modules/tsx/dist/cli.mjs scripts/lockedPresetScenarios.ts`: PASS
- `npm run qa`: PASS via fresh `qa_artifacts/vitest.json` full-suite artifact (`476/476`); integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- The full closure checklist remains `qa_artifacts/playtest_ops/uat_scenario_gate.json`; targeted `--preset` reruns write release-scoped filtered artifacts instead of overwriting the canonical packet.
- Operator review is now explicitly preset-first: launch from the New Run preset selector, confirm run provenance, then compare the matching `closure_checklist[]` row and locked manifest cues for that preset id.
