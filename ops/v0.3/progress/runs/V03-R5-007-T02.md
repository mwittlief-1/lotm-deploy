# Run Log

**Run ID:** V03-R5-007-T02
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-economy-fiscal
**Purpose:** Implement court ration and stipend fiscal policy through canonical provisioning and ledger paths.

## Delivered scope

- Added `src/sim/domains/economy/courtProvisioningFiscal.ts` to derive deterministic ration allocation, stipend policy, undernourishment badges, and receipt-backed stipend application outputs.
- Extended `src/sim/domains/people/courtProvisioningRegistry.ts` to expose fiscal policy fields through the accepted provisioning view and stipend registry without crossing the integrator boundary.
- Added `tests/sim/court_provisioning_fiscal.test.ts` and extended `tests/sim/bounded_snapshot_contract.test.ts` so the new provisioning fiscal seam is fixture-safe and replay-safe.

## Gate status

- Focused vitest suite: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- Ration allocation order is deterministic and badge output is fully derived from the accepted provisioning roster plus current stores.
- Stipend placeholders stay on canonical ledger writes and expose total requested coin for downstream UI and balancing surfaces.
