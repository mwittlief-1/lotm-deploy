# V03-R5-006-T03A - Integrator wiring for maintenance labor drag

## Summary

Accepted the integrator-owned maintenance wiring on `codex/v0.3-refactor-kickoff`.
The preview turn pipeline now applies builder-first maintenance labor drag before
harvest and construction consume allocatable labor, without mutating the
planned farmer/builder assignment rows. The accepted seam also emits the
maintenance pressure summary lines into preview consumption receipts and report
notes so downstream UI explain surfaces have an auditable contract to render.

## Changed Files

- `src/sim/turn.ts`
- `src/sim/phases/phase_consumption.ts`
- `tests/sim/maintenance_labor_wiring.test.ts`

## Validation

- pass: `npx vitest run tests/sim/maintenance_labor_helper.test.ts tests/sim/delegated_maintenance.test.ts tests/sim/maintenance_labor_wiring.test.ts`
- pass: `npm run qa`
- fail: `npm run preflight` with standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-maint-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-maint-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped

## Notes

- The accepted seam uses the existing `_mods` preview mechanism to apply
  deterministic farmer and builder penalties exactly once before
  `applyProductionAndConstructionPhase`.
- The normalized replay summary hash remained
  `82a13540a5939d973347810ae3f75089b1a18d25c195c39889b7afa32b3182bb`, so the
  maintenance wiring did not perturb deterministic replay content.
