# 2026-04-11 - v0.3.5 UI, social, and tooling wave acceptance

## Summary

Accepted the first combined UI/social/tooling bundle onto the `v0.3.5` control
plane after integrating the lane handoffs into the acceptance worktree and
re-running the merge gates. This wave closes the first player-facing map,
manor, house dossier, person-card projection, and maintenance-pressure explain
surfaces, then promotes the next fixture, person-card modal, outbound scouting,
provisioning, obligations, preset seam, and balance-band tasks.

Accepted tasks:

- `V03-R5-001-T04`
- `V03-R5-002-T03`
- `V03-R5-003-T02`
- `V03-R5-004-T01`
- `V03-R5-004-T02`
- `V03-R5-006-T04`
- `V03-R5-006-T05`

## Validation

- pass: fresh `npm run qa` vitest tranche (`421/421` tests) plus fresh `npm run uat:gate`
- fail: `npm run preflight` with the standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-ui-social-tooling-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-ui-social-tooling-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped

## Notes

- The fresh UAT gate remained green, but its six-seed smoke sweep now runs in
  the multi-minute range rather than the shorter locked-baseline envelope.
- Replay soft time-ceiling warnings appeared on the usual long-tail seeds, but
  both batches still passed and normalized to the same content hash
  `b81bffa111387f7dec9a4e1a0ab13834cc6374e0dbca86a18ac458cbe8c283de`.
- The accepted UI burst keeps the map and manor surfaces read-only against the
  accepted world and economy contracts, while the social burst extends the
  bounded snapshot with canonical person-card projections rather than
  cross-domain wiring.
