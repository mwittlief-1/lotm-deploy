# Escalation Report

**Task ID:** V03-R0-002-T05
**Date:** 2026-03-26
**Tier:** E1
**Branch:** codex/v0.3-lane-social-mechanics
**Commit:** f7e232cd07d91c7e85e9afdf544300912d41c253
**PR URL:** 

## What happened
- Claimed `V03-R0-002-T05` after kickoff promoted it to `ready` and the social lane became claimable again.
- Revalidated the control-plane claim with `npm run ops:v0.3:validate -- --json`, `npm run ops:v0.3:scheduler-dry-run -- --json`, and `npm run ops:v0.3:rebase-dry-run -- --json`.
- Audited the current marriage concurrency path before editing implementation code and confirmed the task contract cannot be satisfied from `src/sim/domains/people/**` plus `tests/**` alone.

## Evidence
- Gate results:
  - `ops:v0.3:validate -- --json`: PASS
  - `ops:v0.3:scheduler-dry-run -- --json`: PASS
  - `ops:v0.3:rebase-dry-run -- --json`: PASS
  - `qa`: not run
  - `preflight`: not run
  - `seed_replay_batch_twice`: not run
- Replay hashes:
  - not run for this escalation
- Golden diff summary:
  - no implementation diff was made because the task stopped before code changes
- Environment notes:
  - `src/sim/phases/phase_prospects.ts:292-358` only materializes a single marriage prospect and explicitly blocks a second active marriage prospect with `!activeTypes.has("marriage")`
  - `src/sim/types.ts:402-404` exposes `MarriageWindow` only as `eligible_child_ids` plus one flat `offers` list, with no per-subject offer ownership shape
  - `docs/qa/marriage_offer_audit_v0.3.0.md:157` already notes that `eligible_child_ids` and `_prospects_active_v1` are too narrow if household concurrency is a goal

## Why this is an escalation
- `V03-R0-002-T05` requires multiple household members to hold marriage offers in the same turn, but the current phase wrapper still emits at most one persisted `marriage` prospect per turn.
- Achieving the task's done conditions would require editing integrator-owned `src/sim/phases/phase_prospects.ts` and widening the shared `MarriageWindow` contract in `src/sim/types.ts`, both outside this task's allowed paths.
- `ops/v0.3/escalation-policy.yaml` classifies `orchestrator_edit_without_requires_integrator` as `E1_stop_and_report`, and the v0.3 README says phase surfaces are integrator-only unless the task explicitly allows them.

## Proposed options
1. Re-scope `V03-R0-002-T05` as integrator work, or widen its allowed paths to include `src/sim/phases/**` and the shared type surface needed for per-subject offer ownership.
2. Split `T05` into a domain-only seam task and move the actual same-turn concurrency behavior to the existing integrator step so the backlog matches the current architecture.

## Recommended next action
- Stop the social lane here, clear the active claim, and hand `V03-R0-002-T05` back to kickoff for backlog correction or integrator reassignment before any source edits are attempted.
