# Run Log

**Run ID:** 2026-04-10-v0.3.5-world-engine-acceptance-round2
**Date:** 2026-04-10
**Branch:** codex/tmp-v0.3.5-acceptance
**Purpose:** Accept the packaged engine helper seam plus the opening world map/topology handoff onto the `v0.3.5` control-plane line.

## Accepted lane commits

### Engine core

- Source branch: `codex/v0.3-lane-engine-core`
- Accepted commit: `355d88e`
- Accepted task:
  - `V03-R5-006-T03`

### World topology

- Source branch: `codex/tmp-v0.3.5-world-topology-handoff`
- Source commit: `23b6a42`
- Accepted tasks:
  - `V03-R5-001-T02`
  - `V03-R5-001-T03`
  - `V03-R5-002-T01`
  - `V03-R5-005-T01`

## Integration adjustments

- Preserved the already-accepted `economy_maintenance_view` seam while merging world snapshot changes.
- Split rich map selector data from the serialized `map_view_snapshot_v1` rows so replay snapshots remain under the byte cap.
- Kept `manor_detail_view` as a world-owned projection seam rather than serializing it into every bounded replay snapshot.

## Gate results

- Targeted QA rerun over all touched suites
  - PASS
  - `34/34` tests passed across world, bounded snapshot, replay-bound, engine helper, UI route, and ops control-plane suites
- Full `qa` wrapper
  - wrapper remained sticky in the temp worktree after an earlier artifact run exposed a single failing allowlist assertion in `tests/logSnapshotsBounded.test.ts`
  - that exact failing suite and all affected world/map suites reran green after the compact snapshot fix
- `npm run preflight`
  - FAIL
  - unchanged standing baseline blocker:
    - `test3_mismatches=16`
- `npm run seed:replay:batch` twice with comparison
  - PASS
  - replay summary hash, run 1: `06954e214d0486ef1546375ef7697428df426fe585b78b615f1a8d48071568c7`
  - replay summary hash, run 2: `06954e214d0486ef1546375ef7697428df426fe585b78b615f1a8d48071568c7`
  - artifact trees from both runs matched under `diff -rq`

## Acceptance decision

- Accept the engine helper seam and compacted world map/topology seams onto the `v0.3.5` control-plane line.
- Treat the `preflight` mismatch set as the standing baseline blocker, not an incremental regression from this round.
- Re-open UI as the primary live lane on the newly accepted map and manor seams.
