# V03-R5-004-T01 - Define person_card_view_v1 from registries and summaries

## Summary

Rebased the social lane onto `codex/v0.3.5-prep` at `7e2d603` and added the canonical `person_card_view_v1` / `person_card_registry_v1` read model from existing people, kinship, residence, court, and succession seams. The preview-state and bounded-snapshot surfaces now attach hidden person-card projections without changing the serialized snapshot contract.

## Changed Files

- src/sim/domains/experience/reporting.ts
- src/sim/domains/people/personCardRegistry.ts
- src/sim/domains/people/playerHousehold.ts
- src/sim/types.ts
- tests/sim/bounded_snapshot_contract.test.ts
- tests/sim/person_card_registry.test.ts

## Notes

- Kept the current prep economy/world seams in `reporting.ts`, including maintenance and bounded map/topology snapshot behavior.
- Did not carry the older provisioning or UI changes from the opening-wave merge.
- Office and service projections fall back to existing legacy court seams when the newer court office registry is absent, so the social lane stays inside its allowed write scope.

## Verification

- pass: npx vitest run tests/sim/person_card_registry.test.ts tests/sim/bounded_snapshot_contract.test.ts tests/sim/succession_summary_surfaces.test.ts
- blocked: npm run qa (runner hung idle without producing a fresh `qa_artifacts/vitest.json`; the run was stopped instead of being left orphaned)
- fail: npm run preflight (`test3_mismatches=16`)
- pass: npm run seed:replay:batch -- --outdir=qa_artifacts/seed-replay-r5004-run1
- pass: npm run seed:replay:batch -- --outdir=qa_artifacts/seed-replay-r5004-run2
- pass: normalized replay summaries match exactly across both batch runs; differing `summary_hash` values were only due to the output directory path baked into `artifact_relpath`
- note: both replay batches produced widespread soft timing warnings against the `250ms` per-seed warning ceiling, but still completed successfully
