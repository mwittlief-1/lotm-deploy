# Run Log

**Run ID:** V03-R5-003-T03
**Date:** 2026-04-12
**Branch:** codex/v0.3-lane-tooling-qa
**Purpose:** Lock the accepted dossier contract with deterministic fixtures and explicit prospect completeness checks.

## Delivered scope

- Added `tests/fixtures/house_dossier_snapshot_v0.3.5.json` with one known-house case plus prospect-house cases for marriage and grant flows.
- Added `tests/ui/houseDossierFixtures.test.ts` to assert fixture stability and require every marriage/grant prospect house to resolve to a populated dossier surface.
- Documented the fixture and completeness rule in `docs/qa/house_dossier_fixtures_v0.3.5.md`.

## Gate status

- Focused vitest suite: PASS
- `npm run qa`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run preflight`: not run on the lane after packaging; integrator should rerun merge gates
- `npm run seed:replay:batch`: not run on the lane after packaging; integrator should rerun merge gates

## Contract handoff

- The completeness rule rebuilds dossier surfaces through the accepted `house_dossiers` contract and reattached `prospects_window`, rather than introducing a UI-only adapter.
- Debug-row key order is locked so later dossier deepening can be compared deterministically.
