# V03-R5-004-T02 - Add stable family, succession, lands, and service projections

## Summary

The `V03-R5-004` chain stayed clean on top of `7e2d603`, so the lane self-chained from `T01` into the deeper projection work in the same rewrite. Person cards now expose deterministic family references, married-out state, succession/claim projections, lands-held summaries, and service timeline entries keyed by stable person ids.

## Changed Files

- src/sim/domains/people/personCardRegistry.ts
- src/sim/domains/people/successionSummaries.ts
- src/sim/types.ts
- tests/sim/person_card_registry.test.ts
- tests/sim/succession_summary_surfaces.test.ts

## Notes

- Added `adult_line_position` to succession summary entries so person cards can distinguish the adult line from the full line.
- Lands-held projections reuse the accepted bounded holdings semantics from the dossier seam.
- Service timelines remain deterministic by sorting on turn/index keys and stable record ids.

## Verification

- pass: npx vitest run tests/sim/person_card_registry.test.ts tests/sim/bounded_snapshot_contract.test.ts tests/sim/succession_summary_surfaces.test.ts
- blocked: npm run qa (runner hung idle without producing a fresh `qa_artifacts/vitest.json`; the run was stopped instead of being left orphaned)
- fail: npm run preflight (`test3_mismatches=16`)
- pass: npm run seed:replay:batch -- --outdir=qa_artifacts/seed-replay-r5004-run1
- pass: npm run seed:replay:batch -- --outdir=qa_artifacts/seed-replay-r5004-run2
- pass: normalized replay summaries match exactly across both batch runs; differing `summary_hash` values were only due to the output directory path baked into `artifact_relpath`
- note: both replay batches produced widespread soft timing warnings against the `250ms` per-seed warning ceiling, but still completed successfully
