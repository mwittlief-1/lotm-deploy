# V03-R4-006-T01 - Expose gesture actions and structured raw receipts in the UI

## Summary

Completed the player-visible activation pass for obligations gestures and Raw receipts on the UI lane. The live decisions surface now exposes gift-to-liege and offering-to-church controls using the existing gesture contract fields, and the Raw receipts view now renders structured `fiscal_receipts_v1` rows when present while preserving the legacy string fallback.

## Changed Files

- `src/ui/panels/DecisionsPanel.tsx`
- `src/ui/panels/ReceiptsViewerPanel.tsx`
- `src/ui/playScreenObligations.ts`
- `src/ui/playScreenReceipts.ts`
- `tests/ui/decisionsPanelCourtBudget.test.tsx`
- `tests/ui/playScreenObligations.test.ts`
- `tests/ui/playScreenReceipts.test.ts`
- `tests/ui/receiptsViewerPanel.test.tsx`

## Gate Summary

- feature commit: `0e3e0c75744f334063d94541b2b636e664e43fa5`
- pass: `npm test -- --run tests/ui/playScreenObligations.test.ts tests/ui/decisionsPanelCourtBudget.test.tsx tests/ui/playScreenReceipts.test.ts tests/ui/receiptsViewerPanel.test.tsx`
- fail: `npm run preflight`
- pass: `npm run seed:replay:batch`
- pass: `npm run seed:replay:batch`
- blocked-clean-exit: `npm run qa`
- blocked-clean-exit: `npm run uat:gate`

## Replay

- `run1`: `02250274c13fafacef3f545fdb5f0ddc5410e0c910ceb14fb01babb7b9723379`
- `run2`: `02250274c13fafacef3f545fdb5f0ddc5410e0c910ceb14fb01babb7b9723379`

## Notes

- `preflight` failed on refreshed kickoff truth with `test3_mismatches=16` in `qa_artifacts/v0.3.4_preflight.json`; the first reported mismatches were the prudent-builder seeds `lotm_v022_seed_001_baseline_extworld`, `lotm_v022_seed_002_relationship_edges`, and `lotm_v022_seed_003_succession_pressure`.
- Replay stayed deterministic across two full batch runs, so the UI patch did not introduce a replay hash split on the current lane branch state.
- The UI work kept the shared contract field names unchanged: `decisions.obligations.gestures.gift_liege`, `decisions.obligations.gestures.offering_church`, and `phaseResult.fiscal_receipts_v1`.
- The decisions surface keeps the existing `pay_coin` and `pay_bushels` controls intact and adds gesture planning as a separate visible control block.
- The Raw receipts surface now prefers structured fiscal rows and renders `receipt_id`, `category`, `asset`, `delta`, `counterparty label`, and `summary / rule` data when those rows are present.
- The `qa` and `uat:gate` processes did not return a clean exit in this workspace after long-running execution, so they should be treated as not cleanly verified here rather than silently assumed to pass.
