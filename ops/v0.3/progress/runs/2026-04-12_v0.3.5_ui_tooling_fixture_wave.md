# 2026-04-12 - v0.3.5 UI and tooling fixture wave

## Summary

Accepted the next `UI` and `tooling` wave onto the `v0.3.5` control-plane line
after integrating the lane packets into the acceptance worktree and rerunning
the deterministic merge-side checks.

Accepted tasks:

- `V03-R5-005-T05`
- `V03-R5-007-T04`

## Validation

- pass: merged focused acceptance bundle
  (`23/23` tests across outbound marriage and court-provisioning surfaces)
- fail: `npm run preflight` with the standing baseline blocker `test3_mismatches=16`
- pass: replay batch 1 via
  `node /Users/matt_wittlief_home/Documents/GitHub/lotm-deploy/node_modules/tsx/dist/cli.mjs scripts/seedReplay.ts --mode=batch --outdir=/private/tmp/lotm-r5-wave5-pass1`
- pass: replay batch 2 via
  `node /Users/matt_wittlief_home/Documents/GitHub/lotm-deploy/node_modules/tsx/dist/cli.mjs scripts/seedReplay.ts --mode=batch --outdir=/private/tmp/lotm-r5-wave5-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped
- warning: fresh full-suite `qa` and direct `uatGate` reruns were attempted but
  both runner paths hung in this environment without producing fresh artifacts;
  the last known accepted `qa_artifacts/uat_gate.json` remains green

## Notes

- The accepted UI packet renders the outbound marriage scouting sheet, candidate
  table, offer composer, and debug preview against the canonical scouting and
  offer-resolution seams without mutating live sim state.
- The accepted tooling packet locks the court provisioning sheet with compact
  deterministic fixtures and a reviewable UAT pack anchored to the accepted
  provisioning surface.
- The raw replay summary hashes differed
  (`1a3b82ab4cdbe19049f2dea1a363bd5043cffb5be75821ec11b5e06bfaa7d9d9` and
  `47dafc7c5f5e5436c0bf9e56e314a913278e69c9b25d581630df1eea58524eb4`), but the
  normalized hash matched exactly at
  `e519f5ce476960d955e228662db3b9d8e9f20594d60b7c217e72524b20bfb568`.
