# Run Log

**Run ID:** V03-R6-008-T05
**Date:** 2026-04-20
**Branch:** `codex/v0.3-refactor-kickoff`
**Commit:** `eb8eaad`

## Delivery state

- state: accepted
- summary: compacted replay snapshots for the known succession-pressure cap offender without removing accepted world-map functionality from the live player path.

## What changed

- moved `manor_detail_view` off the serialized bounded snapshot and onto a hidden non-enumerable property so the read model remains accessible without being replay-budget payload
- removed duplicated `manor_label` strings from serialized `map_view_snapshot` rows
- updated world-map fallback label handling so markers, debug rows, and route targets still render the same player-facing labels from selector rows or seat hex ids
- refreshed bounded snapshot and world-map fixture coverage to lock the compacted shape

## Evidence

- reproduced the original failure on kickoff:
  - `npm run seed:replay:batch` failed on `lotm_v022_seed_003_succession_pressure / builder-forward / turn 13 / 528401 bytes / cap 524288`
- after the first trim (`manor_detail_view` hidden):
  - the same path at turn 13 dropped to `505660` bytes
  - full batch advanced far enough to expose a later offender on the same seed and policy at `turn 15 / 526672 bytes`
- after the second trim (`map_view_snapshot` row compaction):
  - `lotm_v022_seed_003_succession_pressure / builder-forward / turn 13` dropped to `495985` bytes
  - `lotm_v022_seed_003_succession_pressure / builder-forward / turn 15` dropped to `516997` bytes
  - `lotm_v022_seed_003_succession_pressure / prudent-builder / turn 15` stayed under cap at `514434` bytes
- replay gate:
  - `npm run seed:replay:batch` -> pass
  - `npm run seed:replay:batch` -> pass
  - `summary_hash` on both runs: `4c3f0f38d3a6796b60640e9b180f674b2d4fa63b14486be001cfb274bc77095c`
  - evidence path: `qa_artifacts/seed_replay/v0.3.4/batch/turns_15/summary.json`
- focused regression pack:
  - `npm exec vitest run tests/sim/bounded_snapshot_contract.test.ts tests/logSnapshotsBounded.test.ts tests/sim/world_map_surfaces.test.ts` -> pass (`7/7`)

## Handoff

- accepted because:
  - the known replay-cap offender is now below the configured byte ceiling
  - full batch replay passes twice with matching hash evidence
  - accepted player-facing world-map and manor-detail behavior remains available on the live path
- remaining blockers for release closeout:
  - `npm run preflight` still fails with `test3_mismatches=16`
  - `npm run qa` still does not have a clean branch-wide exit recorded on kickoff
