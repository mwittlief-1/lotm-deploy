# 2026-04-10 v0.3.5 Lane Launch Pack

## Readiness Call

- `v0.3.4` carry-over is now effectively closed on repo truth:
  - `V03-R4-004-T01`: done
  - `V03-R4-005-T01`: done
  - `V03-R4-006-T01`: done
- That means the old carry-over gate is no longer the blocker.
- The remaining blocker is control-plane formality:
  - `ops/v0.3/backlog.yaml` does not yet contain canonical executable `V03-R5-*` tasks
  - branch policy still treats `codex/v0.3-refactor-kickoff` as the canonical integrator branch, while the current `v0.3.5` planning state lives on `codex/v0.3.5-prep`

## Immediate Recommendation

- Do not wait on old lane chats.
- Start fresh lane chats now using the launch messages below, but treat them as the opening `v0.3.5` work wave and not yet as scheduler-backed `V03-R5-*` execution.
- Base the opening lane work from `codex/v0.3.5-prep` unless and until integrator promotes that branch back onto the canonical kickoff line.

## Opening Wave

### World Topology

- First assignment:
  - map snapshot seam and selectors
- Consecutive tasks available before another lane blocks progress:
  - `4`
- Sequence:
  - `R5-001-T02`
  - `R5-001-T03`
  - `R5-002-T01`
  - `R5-005-T01`
- First external blocker after that:
  - UI for map rendering and route wiring
  - Social for outbound scouting semantics

### Economy Fiscal

- First assignment:
  - maintenance registry, manor maintenance projection, obligations deep payload
- Consecutive tasks available before another lane blocks progress:
  - `4`
- Sequence:
  - `R5-006-T01`
  - `R5-006-T02`
  - `R5-002-T02`
  - `R5-008-T01`
- First external blocker after that:
  - Engine core for maintenance labor drag
  - Social for outbound marriage and provisioning follow-ons

### Social Mechanics

- First assignment:
  - dossier semantics, person-card semantics, provisioning input surfaces
- Consecutive tasks available before another lane blocks progress:
  - `4` guaranteed
  - `5` if world finishes `R5-005-T01` in time
- Guaranteed sequence:
  - `R5-003-T01`
  - `R5-004-T01`
  - `R5-004-T02`
  - `R5-007-T01`
- Optional fifth task when world is ready:
  - `R5-005-T02`
- First external blocker after that:
  - Economy for dowry or dower receipt scaffolds

### UI Experience

- First assignment:
  - no opening-wave work until world and social seams land
- Consecutive tasks available before another lane blocks progress:
  - `2` in the first clean burst
  - later more work unlocks in batches
- First clean burst:
  - `R5-003-T02`
  - `R5-004-T03`
- Later burst after world and economy map or manor seams land:
  - `R5-001-T04`
  - `R5-002-T03`
- Later burst after social and economy deeper seams land:
  - `R5-005-T05`
  - `R5-007-T03`
  - `R5-008-T02`
  - `R5-009-T04`

### Engine Core

- First assignment:
  - maintenance labor drag only
- Consecutive tasks available before another lane blocks progress:
  - `1`
- Sequence:
  - `R5-006-T03`
- First external blocker after that:
  - UI and Tooling consume the new seam

### Tooling QA

- First assignment:
  - preset-pack schema and launch docs
- Consecutive tasks available before another lane blocks progress:
  - `1` immediate
  - later multiple short bursts
- Immediate task:
  - `R5-009-T01`
- Next bursts:
  - after UI map: `R5-001-T05`
  - after UI dossier: `R5-003-T03`
  - after UI person cards: `R5-004-T04`
  - after engine maintenance drag: `R5-006-T05`
  - after UI obligations: `R5-008-T03`
  - after UI outbound marriage: `R5-005-T06`
  - after UI provisioning: `R5-007-T04`
  - final preset lock burst:
    - `R5-009-T03`
    - `R5-009-T05`

## Fresh Chat Messages

### World Topology chat

```text
You are opening the first v0.3.5 world-topology lane pass from the clean v0.3.4 locked baseline plus the v0.3.5 prep planning branch.

Base branch: codex/v0.3.5-prep
Lane branch: codex/v0.3-lane-world-topology

Mission:
Implement the world-owned opening wave for v0.3.5 without touching src/sim/turn.ts.

Your owned sequence is:
1. R5-001-T02: vendor/attach the approved map data seam details needed for map view selectors, including manor seat q,r lookup, owner/holding/county labels, and explicit empty-state behavior for optional road/river exposure.
2. R5-001-T03: expose map_view_snapshot_v1 through bounded snapshots using existing portfolio/map checkpoint semantics.
3. R5-002-T01: expose manor_detail_view_v1 with hex_count, arability summary, terrain mix, nearest-manor rows, and per-hex table rows.
4. R5-005-T01: expose resolveActionScope(manor_id, action_type) using existing topology caps and residence hooks.

Guardrails:
- Do not edit src/sim/turn.ts.
- Keep ordering deterministic and row ordering stable.
- Do not invent UI presentation logic in this lane.
- Reuse existing world/topology registries and bounded snapshot conventions.

Deliverables:
- code
- tests/fixtures for deterministic ordering and snapshot shape
- a concise handoff note with changed files, gates run, and any blockers for UI/social follow-on work
```

### Economy Fiscal chat

```text
You are opening the first v0.3.5 economy-fiscal lane pass from the clean v0.3.4 locked baseline plus the v0.3.5 prep planning branch.

Base branch: codex/v0.3.5-prep
Lane branch: codex/v0.3-lane-economy-fiscal

Mission:
Implement the economy-owned opening wave for v0.3.5 with receipt-grade, deterministic write paths only.

Your owned sequence is:
1. R5-006-T01: audit live buildings, rights, court-maintenance scaffolds, and tuning hooks; define maintenance_registry_v1 and receipt taxonomy.
2. R5-006-T02: implement recurring coin cost and labor requirement per building/right through canonical APIs.
3. R5-002-T02: add the read-only manor maintenance/buildings/rights summary projection keyed by manor, reusing maintenance_registry_v1.
4. R5-008-T01: extend economy_obligations_view with accepted payment modes, next-stage trigger fields, bite-preview data, and structured payment/penalty/seizure receipt grouping.

Explicit tightenings:
- Maintenance labor drag must be auditable downstream; do not hide required data.
- Obligations payload must make terminal-risk transparency possible later in UI.
- Prefer typed receipts or deterministic registries over prose-only notes.

Guardrails:
- Do not edit src/sim/turn.ts.
- All coin/store deltas stay inside canonical economy APIs.
- Keep stable ordering and deterministic receipt emission.

Deliverables:
- code
- focused tests
- a handoff note with changed files, gates run, and precise seam notes for engine/UI follow-on work
```

### Social Mechanics chat

```text
You are opening the first v0.3.5 social-mechanics lane pass from the clean v0.3.4 locked baseline plus the v0.3.5 prep planning branch.

Base branch: codex/v0.3.5-prep
Lane branch: codex/v0.3-lane-social-mechanics

Mission:
Implement the opening people/social semantics wave for v0.3.5 without touching integrator-only orchestration.

Your guaranteed opening sequence is:
1. R5-003-T01: extend known-house surfaces to house_dossier_v2 with knownness, A/R/T summary, holdings footprint, kinship tags, coarse ledger band/trend, and prospect-house dossier invariants.
2. R5-004-T01: define person_card_view_v1 from people registry, kinship, succession summaries, residence-manor bindings, offices, and service records.
3. R5-004-T02: add stable family, claims/succession, married-out, lands-held, and service-timeline projections.
4. R5-007-T01: define per-court-member provisioning view and stipend registry keyed to stable person IDs and prior-turn defaults.

Optional fifth task if world has already delivered resolveActionScope:
5. R5-005-T02: replace scout-buff-only behavior with outbound scouting registry, bounded candidate ranking, and eligibility include/exclude reasons.

Guardrails:
- No direct relationship-store writes outside canonical relationship APIs.
- No UI implementation in this lane.
- Keep projections deterministic and stably ordered.

Deliverables:
- code
- fixtures/tests
- a handoff note with blockers and explicit contract details needed by UI and economy
```

### UI Experience chat

```text
You are opening a fresh v0.3.5 UI-experience lane chat from the clean v0.3.4 locked baseline plus the v0.3.5 prep planning branch.

Base branch: codex/v0.3.5-prep
Lane branch: codex/v0.3-lane-ui-experience

Mission:
Stand by for the first v0.3.5 UI burst and be ready to self-chain when upstream seams land. Do not invent contracts that upstream lanes are already defining.

Opening work priority once social seams are available:
1. R5-003-T02: replace summary-only drilldown with house dossier modal plus debug table tab.
2. R5-004-T03: wire tap-any-name person-card modal from household, roster, prospects, known houses, and dossier surfaces.

Second burst once world/economy map and manor seams are available:
3. R5-001-T04: render the kingdom map, highlight player holdings and liege seat, route manor clicks into Manor View v0, and show the debug table overlay sorted by manor_id.
4. R5-002-T03: render Manor View player tab plus debug tab, reachable from map and holdings selector, with nearby-manor list and hex table.

Later burst:
- R5-005-T05
- R5-007-T03
- R5-008-T02
- R5-009-T04

Guardrails:
- Keep state mutation out of UI.
- Consume bounded contracts rather than reaching into raw sim state when a read model exists.
- Preserve deterministic row and table ordering.

Deliverables:
- code
- component tests
- concise handoff notes after each accepted burst
```

### Engine Core chat

```text
You are opening the narrow v0.3.5 engine-core lane pass from the clean v0.3.4 locked baseline plus the v0.3.5 prep planning branch.

Base branch: codex/v0.3.5-prep
Lane branch: codex/v0.3-lane-engine-core

Mission:
Implement only the narrow runtime seam that cannot live cleanly in an owned domain lane.

Your task:
1. R5-006-T03: apply maintenance labor drag before allocatable farming/building labor is consumed, keeping stable ordering and making the pressure auditable.

Explicit tightening:
- Do not make maintenance labor drag feel magical.
- Emit either typed receipts or a deterministic capacity-pressure registry/snapshot that downstream UI/tooling can surface.

Guardrails:
- Keep src/sim/turn.ts orchestration changes minimal and only if unavoidable.
- Preserve deterministic ordering and replay behavior.
- Coordinate with economy-fiscal seam ownership rather than re-defining maintenance semantics locally.

Deliverables:
- code
- focused tests
- a handoff note describing exactly what downstream UI/tooling should consume
```

### Tooling QA chat

```text
You are opening the first v0.3.5 tooling-qa lane pass from the clean v0.3.4 locked baseline plus the v0.3.5 prep planning branch.

Base branch: codex/v0.3.5-prep
Lane branch: codex/v0.3-lane-tooling-qa

Mission:
Own the deterministic test/fixture/control-plane side of v0.3.5 closure, starting with presets and then consuming new UI seams as they land.

Immediate task:
1. R5-009-T01: define playability_preset_pack_v1, stable IDs, and scenario-to-acceptance mapping; extend existing seed-pack conventions instead of creating a second control plane.

Later self-chaining tasks as upstream work lands:
- R5-001-T05
- R5-003-T03
- R5-004-T04
- R5-006-T05
- R5-008-T03
- R5-005-T06
- R5-007-T04
- R5-009-T03
- R5-009-T05

Explicit tightening:
- v0.3.5 should add KPI acceptance bands and runaway detectors, not just surface-level UAT presets.
- Preserve one deterministic preset application seam and one seed-pack convention.

Guardrails:
- Do not create a second initialization control plane.
- Prefer deterministic fixtures, replay guards, and contract checks over ad hoc manual notes.

Deliverables:
- fixtures/docs/scripts/tests as appropriate
- a handoff note showing exact artifacts added and what upstream/downstream tasks they unlock
```

## Integrator Next Step

- Canonical scheduler readiness still needs one integrator pass:
  - either promote `codex/v0.3.5-prep` back onto the kickoff control-plane line
  - or author `V03-R5-*` tasks directly on top of `codex/v0.3.5-prep` and then cut lane execution from there
- Until that is done, the messages above are valid as human dispatch packets, but not yet as canonical `backlog.yaml` task execution.
