# 2026-04-13 - v0.3.5 preset UAT closure acceptance

## Summary

Accepted the `tooling`-owned preset UAT closure checklist onto the `v0.3.5`
control-plane line after integrating the lane packet into the acceptance
worktree and rerunning the deterministic merge-side checks.

Accepted task:

- `V03-R5-009-T05`

## Validation

- pass: merged focused script validation
  (`scripts/uatScenarioGate.ts`, the three `--preset` reruns, and
  `scripts/lockedPresetScenarios.ts`)
- fail: `npm run preflight` with the standing baseline blocker `test3_mismatches=16`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave9-pass1`
- pass: `npm run seed:replay:batch -- --outdir=/private/tmp/lotm-r5-wave9-pass2`
- pass: normalized replay summaries match exactly once `hash` and
  `artifact_relpath` metadata are stripped
- warning: fresh full-suite `qa` was attempted, but the runner hung in this
  environment without producing a fresh merge-side result and was terminated

## Notes

- The accepted packet keeps `qa_artifacts/playtest_ops/uat_scenario_gate.json`
  as the canonical preset-backed closure checklist and writes
  release-scoped filtered artifacts for targeted `--preset` reruns instead of
  replacing the full packet.
- Operator review is now explicitly preset-first: launch from the New Run
  preset selector, confirm provenance, and compare the matching
  `closure_checklist[]` row plus locked manifest cues for that preset id.
- The raw replay summary hashes differed
  (`ae452936059c6c24c4870f14f9a9bab98bb1747cbc2b2297d7646906ce2b09d7` and
  `99ad5e28eabfa942b813e5f7dd12f3c4437626e07949c200373823c05bb0faef`), but the
  normalized hash matched exactly at
  `e519f5ce476960d955e228662db3b9d8e9f20594d60b7c217e72524b20bfb568`.
